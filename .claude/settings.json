{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'F=$(jq -r \".file_path // empty\" 2>/dev/null); if [[ -n \"$F\" ]] && [[ \"$F\" == *.env* || \"$F\" == *dev.vars* ]]; then echo \"BLOCKED: Editing secret files (.env*, .dev.vars) is not allowed\" >&2; exit 2; fi'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'F=$(jq -r \".file_path // empty\" 2>/dev/null); if [[ \"$F\" =~ \\.(tsx?|jsx?|mjs)$ ]] && [[ \"$F\" != *node_modules* ]]; then npx eslint --fix --no-warn-ignored \"$F\" 2>/dev/null; fi; exit 0'"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh"
          }
        ]
      }
    ]
  }
}
