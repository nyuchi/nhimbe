{
  "name": "nhimbe-mcp",
  "version": "2.0.0-beta.1",
  "private": true,
  "description": "nhimbe task-based MCP server (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'Build handled by wrangler deploy'",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "4.110.0"
  },
  "dependencies": {
    "hono": "4.12.25"
  }
}
