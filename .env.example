# nhimbe — environment variables
#
# Copy to `.env.local` for local dev and set the values below. In Vercel these
# are configured per environment (Production + Preview). Values here are
# placeholders — never commit real secrets.
#
# Server-only vars have NO `NEXT_PUBLIC_` prefix and are never exposed to the
# browser bundle. Only `NEXT_PUBLIC_*` vars reach the client.

# --- Data (required) -------------------------------------------------------
# MongoDB connection string (Mukoko v3.1 cluster). Required in prod + preview.
MONGODB_URI=

# --- Auth: WorkOS AuthKit (required) --------------------------------------
# IMPORTANT: WORKOS_CLIENT_ID, WORKOS_API_KEY and the WORKOS_API_HOSTNAME
# custom domain must all belong to the SAME WorkOS environment. A split
# across environments makes Magic Auth codes send but fail verification
# ("code rejected"). API keys are environment-scoped; the client id is an
# application within that environment.
WORKOS_CLIENT_ID=client_...
WORKOS_API_KEY=sk_...
# Session-cookie encryption key, >= 32 chars.
WORKOS_COOKIE_PASSWORD=
# Optional. Defaults to api.workos.com; set to authenticate.nyuchi.com to
# route WorkOS API calls through the Nyuchi custom API domain. (The hosted
# AuthKit sign-in UI is served separately from identity.nyuchi.com, configured
# in the WorkOS dashboard — not via this variable.)
WORKOS_API_HOSTNAME=authenticate.nyuchi.com
# Usually ${NEXT_PUBLIC_SITE_URL}/callback. The NEXT_PUBLIC_ prefix is required.
NEXT_PUBLIC_WORKOS_REDIRECT_URI=https://nhimbe.com/callback

# --- AI: Shamwari (Cloudflare AI Gateway) ---------------------------------
SHAMWARI_AI_GATEWAY_URL=
SHAMWARI_AI_GATEWAY_TOKEN=
# Optional — only for the authenticated gateway.
SHAMWARI_AI_GATEWAY_AUTH_TOKEN=

# --- Email: Resend (optional) ---------------------------------------------
# When unset, email sends are skipped (never throw). Transactional mail is sent
# from the verified `notify.mukoko.com` domain (events@notify.mukoko.com) —
# see src/lib/email/resend.ts.
RESEND_API_KEY=

# --- Public site config ----------------------------------------------------
NEXT_PUBLIC_SITE_URL=https://nhimbe.com
# Maps + address search run on OpenStreetMap: Leaflet renders key-less OSM
# tiles client-side, and geocoding is DB-first (places.places) then OSM
# Nominatim (nominatim.openstreetmap.org) server-side — no Maps API key needed.
# Weather is the shared Mukoko widget embed (weather.mukoko.com/embed/widget).
# Optional — override the R2 assets host (defaults to https://assets-s001.mukoko.com).
NEXT_PUBLIC_ASSETS_URL=

# --- Media uploads: Cloudflare R2 (server-only) ---------------------------
# Cover-image uploads (POST /api/media/upload) write to the shared Mukoko R2
# bucket, read back via NEXT_PUBLIC_ASSETS_URL. Reads need no credentials;
# uploads need an R2 S3 API token. When unset, uploads return 503 and the
# create-event form falls back to the gradient cover.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
# Optional — defaults to mukoko-storage.
R2_BUCKET=mukoko-storage

# --- Local dev auth bypass (dev only) -------------------------------------
# Set to bypass WorkOS auth locally. Never set in Production/Preview.
DEV_AUTH_BYPASS=
NEXT_PUBLIC_DEV_AUTH_BYPASS=

# --- Intentionally unset ---------------------------------------------------
# NEXT_PUBLIC_API_URL — the internal API is same-origin; leaving this unset
# makes src/lib/api.ts call the same origin. Do not set it.
